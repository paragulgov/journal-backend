export enum Role {
  User = 'user',
  Moderator = 'moderator',
  Admin = 'admin',
}

export enum LikeType {
  Like = 'like',
  Dislike = 'dislike',
}

export enum ArticleStatus {
  published = 'published',
  pending = 'pending',
}
